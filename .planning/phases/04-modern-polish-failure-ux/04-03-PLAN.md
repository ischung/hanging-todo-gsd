---
phase: 04-modern-polish-failure-ux
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - js/app.js
autonomous: false
requirements: [PERS-04]
tags: [app, commit, error-surface, delegated-events, integration]

must_haves:
  truths:
    - "js/app.js에 모듈 변수 storageError가 선언되고 commit()이 try/catch로 set/clear한다"
    - "commit()은 state mutation을 항상 메모리에 적용하고(rollback 안 함) save() 실패 시 storageError에 Error를 캡처한다"
    - "render()가 storageError 값으로 banner.hidden을 토글한다 (null이면 hidden, Error면 노출)"
    - "banner의 [data-action='retry'] 클릭은 commit(state) 재호출, [data-action='dismiss']는 storageError=null + render()"
    - "banner click 위임 listener가 1회만 등록된다 (Phase 2 위임 패턴)"
    - "save() 실패 시 앱 크래시 0 (commit이 catch하여 banner로 surface)"
    - "저장 성공 시 storageError가 null로 클리어되고 banner가 자동 숨김"
  artifacts:
    - path: "js/app.js"
      provides: "storageError 모듈 변수 + commit try/catch + banner 위임 + render banner 토글"
      contains: "let storageError"
  key_links:
    - from: "save() throw"
      to: "commit() catch -> storageError = err -> render() -> banner.hidden = false"
      via: "try/catch + render fan-out"
      pattern: "catch\\s*\\(err\\)\\s*\\{\\s*storageError\\s*=\\s*err"
    - from: "[data-action='retry'] click"
      to: "commit(state) 재시도"
      via: "banner click 위임"
      pattern: "action\\s*===\\s*['\"]retry['\"]"
    - from: "render()"
      to: "banner.hidden 토글"
      via: "storageError == null ? hidden : visible"
      pattern: "banner\\.hidden\\s*="
---

<objective>
js/app.js에 storage 실패 흐름을 통합 와이어링한다. 모듈 변수 storageError 추가, commit()을 try/catch로 확장, render()에 banner 토글 한 줄 추가, banner click 위임 listener 추가([retry][dismiss] data-action 분기). 통합 시점에 사람 시각 검증을 포함한다(banner 등장/숨김/재시도 흐름은 자동 grep만으로 부족).

본 plan은 Plan 04-02(banner 마크업 + save throw)를 선재 조건으로 한다. 04-02가 적용되지 않은 상태에서 본 plan이 실행되면 `document.getElementById('storage-banner')`가 null을 반환하여 마운트 가드가 throw한다.

Purpose: PERS-04의 사용자 visible 실패 알림 회로 완성 — silent fake-save 금지 잠금. STYL-01 transition 토큰이 banner 등장/퇴장에도 자연스럽게 적용됨(Plan 04-01의 `.banner { transition: opacity var(--transition); }`).

Output: js/app.js (수정 — storageError + commit try/catch + banner 마운트 + 위임 listener + render banner 토글) + checkpoint(human-verify).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md
@.planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md
@.planning/phases/02-todo-crud/02-04-SUMMARY.md
@.planning/phases/03-calendar-integration/03-03-SUMMARY.md
@js/app.js
@js/storage.js

Interfaces (Plan 04-02 직후 시점):
- js/storage.js: `save(state): void` (실패 시 throw), `load()/KEY/defaultState` 변경 없음.
- index.html: `<aside id="storage-banner" class="banner banner--danger" hidden role="alert">`가 `<main id="app">` 위에 존재. 내부에 `<button data-action="retry">` / `<button data-action="dismiss">` 보유.
- js/app.js 현재 라인 위치 — 모듈 변수 L23-26, root 마운트 L65-66, commit L69-75, render L77-82, calendarSection 위임 L86-110, form submit L112-119.
</context>

<tasks>

<task type="auto">
  <name>Task 1: storageError 변수 + banner 마운트 + commit try/catch + render 토글 + banner 위임</name>
  <read_first>
    - js/app.js (전체 192줄)
    - .planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md (decision D 의사코드 verbatim)
    - .planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md (`js/app.js (MODIFY)` 섹션 — verbatim 코드)
    - js/storage.js (Plan 04-02 직후 — save() void+throw 확인)
    - index.html (Plan 04-02 직후 — `#storage-banner` 노드 존재 확인)
  </read_first>
  <action>
js/app.js를 다음과 같이 수정한다 (in-place — PATTERNS verbatim):

(1) 모듈 변수 추가 — 현재 L23-26 (state/viewYM/selectedKey/editingId) 직후에 한 줄 추가:

```
let storageError = null;  // Phase 4 (PERS-04): save() 실패 시 Error 캡처, 성공/닫기 시 null.
```

(2) banner 마운트 캡처 — 현재 L65-66 (root 캡처 + 가드) 직후에 추가:

```
const banner = document.getElementById('storage-banner');
if (!banner) throw new Error('[hansung-todo] #storage-banner 마운트 노드가 없습니다. index.html에 <aside id="storage-banner">가 <main id="app"> 위에 있는지 확인하세요.');
```

(3) commit() 본문 교체 — 현재 L69-75를 다음으로 정확히 교체 (CONTEXT decision D verbatim):

```
function commit(nextState) {
  state = nextState;                  // (1) 메모리는 항상 적용 (UI 즉시 반영, rollback 안 함)
  try {
    save(state);                      // (2) persist 시도 (실패 시 throw)
    storageError = null;              // (3) 성공 -> 에러 클리어 (이전 실패 banner도 자동 사라짐)
  } catch (err) {
    storageError = err;               // (4) 에러 캡처 -- render()가 banner 토글
    console.warn('[hansung-todo] save failed; kept in memory', err);
  }
  render();                           // (5) banner 노출/숨김 결정은 render에서 storageError로
}
```

(4) render() 확장 — 현재 L77-82 render 본문의 마지막 줄(renderTodoList(...) 다음)에 다음 두 줄을 닫는 `}` 직전에 추가:

```
  // Phase 4 (PERS-04): storageError 존재 시 banner 노출, 아니면 숨김.
  banner.hidden = storageError == null;
```

(5) banner 위임 listener 추가 — 기존 calendarSection 위임 블록(L86-110) **다음**, form submit(L112) **이전**에 다음 블록 삽입:

```
// Phase 4 (PERS-04): banner [재시도][닫기] 위임 — 1회 등록 (Phase 2/3 위임 패턴 동일).
banner.addEventListener('click', (e) => {
  const t = e.target;
  if (!(t instanceof HTMLElement)) return;
  const action = t.closest('[data-action]')?.dataset.action;
  if (action === 'retry') {
    // 현재 in-memory state를 그대로 재저장 — commit이 try/catch + render까지 처리.
    commit(state);
  } else if (action === 'dismiss') {
    storageError = null;
    render();
  }
});
```

변경 금지 영역:
- L1-22 import 블록 + Phase 1 dev hooks 보존.
- form submit 핸들러(L112-119) 보존.
- listContainer change/click/keydown/focusout 위임(L122-158) 보존.
- startEdit/commitEdit/cancelEdit 함수(L160-189) 보존.
- 마지막 `render();` (L191) 보존.
- Phase 1/2 게이트 보존: `js/`에서 `toISOString|toJSON|Date.UTC` 0건, `innerHTML` 0건, `js/storage.js` 외 raw `localStorage.*` 0건.

경계 진실문: 본 task는 `localStorage.*`를 직접 호출하지 않는다. save() throw catch만 사용.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "let storageError" js/app.js; grep -qF "document.getElementById(\x27storage-banner\x27)" js/app.js; grep -qE "catch\s*\(err\)" js/app.js; grep -q "storageError = err" js/app.js; grep -q "storageError = null" js/app.js; grep -qF "banner.hidden = storageError == null" js/app.js; grep -qF "banner.addEventListener(\x27click\x27" js/app.js; grep -qE "action === .retry." js/app.js; grep -qE "action === .dismiss." js/app.js; ! grep -qE "if \(!save\(state\)\)" js/app.js; ! ( grep -E "localStorage\." js/ -r | grep -v "js/storage.js" | grep . ); ! grep -rE "innerHTML|outerHTML|insertAdjacentHTML" js/; ! grep -rE "toISOString|\.toJSON\(|Date\.UTC" js/; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let storageError" js/app.js` 가 1
    - `grep -c "document.getElementById('storage-banner')" js/app.js` 가 1
    - banner 마운트 가드 존재: `grep "if (!banner) throw" js/app.js` 매치 1개
    - commit 본문에 try/catch 존재: `awk '/^function commit/,/^}/' js/app.js | grep -cE "try\s*\{|catch\s*\("` 가 2 이상
    - commit 본문에 boolean 분기 0: `awk '/^function commit/,/^}/' js/app.js | grep -cE "if \(!save"` 가 0
    - render 본문에 banner.hidden 토글 1줄: `awk '/^function render/,/^}/' js/app.js | grep -c "banner.hidden = storageError == null"` 가 1
    - banner 위임 listener 1회 등록: `grep -c "banner.addEventListener('click'" js/app.js` 가 1
    - retry/dismiss 분기 모두 존재: `grep -cE "action === .retry." js/app.js` 가 1, `grep -cE "action === .dismiss." js/app.js` 가 1
    - 회귀 게이트 모두 PASS:
      - `grep -E "localStorage\." js/ -r | grep -v "js/storage.js"` 결과 0건
      - `grep -rE "innerHTML|outerHTML|insertAdjacentHTML" js/` 결과 0건
      - `grep -rE "toISOString|\.toJSON\(|Date\.UTC" js/` 결과 0건
    - app.js 라인 변동: 신규 약 +20줄 (storageError 1, banner 마운트 2, commit 본문 +5, render 본문 +2, banner 위임 ~12).
  </acceptance_criteria>
  <done>commit/render/banner 와이어링 통합 완료. 회귀 게이트 모두 PASS. 마크업+throw+CSS+app.js 4축이 정합.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 시각 검증 — banner 등장/숨김/재시도 흐름</name>
  <what-built>
    Plan 04-01 (CSS 토큰 + banner 룰), Plan 04-02 (banner 마크업 + storage throw), Plan 04-03 (commit try/catch + render 토글 + 위임)이 모두 적용된 상태. localStorage 정상 시 평소처럼 동작하고, save 실패 시 상단 banner가 등장한다.
  </what-built>
  <how-to-verify>
    1. `python3 -m http.server 8000` 실행 후 `http://localhost:8000` 접속.
    2. **시각 폴리시 (STYL-01) 1차 확인:**
       - 시스템 폰트로 한글이 깔끔하게 렌더 (macOS: Apple SD Gothic Neo / Windows: Malgun Gothic).
       - 캘린더 셀 hover 시 부드럽게 색이 바뀐다 (150ms transition).
       - 오늘/선택 날짜 강조 (border + filled bg) 그대로 유지 — 시각 회귀 0.
       - todo 행 line-through, empty 메시지 회색 — 보존됨.
    3. **저장 실패 banner (PERS-04) 시뮬레이션 — DevTools에서:**
       - DevTools Console 열고 다음 실행하여 setItem을 강제 실패시킨다:
         ```
         const orig = Storage.prototype.setItem;
         Storage.prototype.setItem = function() { throw new DOMException('QuotaExceededError', 'QuotaExceededError'); };
         ```
       - 입력창에 "테스트" 입력 + Enter.
       - **기대:** (a) todo는 리스트에 즉시 추가됨 (메모리 반영), (b) 상단에 빨간 좌측 막대 + 옅은 빨강 배경 banner 등장, (c) 본문 "저장에 실패했습니다. 시크릿 모드이거나 저장 공간이 부족할 수 있습니다.", (d) [다시 시도] [닫기] 버튼 노출, (e) 콘솔 에러로 인한 앱 크래시 0.
    4. **재시도 흐름:**
       - banner [다시 시도] 클릭 → setItem이 여전히 실패하므로 banner 유지 (예상).
       - Console에서 setItem 복구: `Storage.prototype.setItem = orig;`
       - banner [다시 시도] 다시 클릭 → banner가 사라짐 (자동 숨김), 새로고침 후에도 "테스트" todo가 그대로 유지(저장 성공).
    5. **닫기 흐름:**
       - 다시 setItem 실패 모드로 전환 + 입력으로 banner 재등장.
       - [닫기] 클릭 → banner 즉시 숨김. (단, 데이터는 여전히 저장 안 됐고 사용자는 그 위험을 인지하고 닫은 상태.)
    6. **시각 회귀 확인:** Phase 2/3에서 작동하던 add/toggle/edit/delete/캘린더 nav/날짜 선택 모두 동일하게 동작.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 이슈 보고. 이슈 시 04-03-PLAN 또는 04-01/04-02 plan 보정 plan 작성.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage.js -> app.js | save() throw가 boundary 통과. err.message는 표준 DOMException 메시지로 PII 미포함. |
| banner click event -> commit/render | data-action 값(retry/dismiss)만 신뢰. 다른 값은 모두 no-op. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | banner data-action 값 | mitigate | 위임 핸들러는 `retry` / `dismiss` 두 값만 처리. 그 외는 no-op. DOM에 다른 data-action을 주입하려면 이미 페이지 통제권을 잃은 상태(다른 XSS 선행 필요). |
| T-04-08 | Information Disclosure | console.warn(err) | accept | dev 환경 콘솔 출력은 사용자 영향 0. 프로덕션 사용자가 보는 banner 본문에는 err 메시지를 textContent로 주입하지 않음(단일 메시지 잠금) — 정보 누출 0. |
| T-04-09 | Denial of Service | save 실패 무한 retry | mitigate | retry는 사용자 액션 트리거(자동 재시도 없음 — CONTEXT D). 무한루프 불가. quota 상황에서 사용자가 스팸 클릭하더라도 setItem 1회/클릭으로 비용 한정. |
| T-04-10 | Tampering | storageError 변수가 모듈-let | accept | 외부 모듈에서 접근 불가(ES module 캡슐화). 사용자가 DevTools로 직접 갱신해도 자기 환경에 한정. |
| T-04-11 | Repudiation | dismiss 후 데이터 손실 | accept | UX 디자인 결정(CONTEXT D) — 사용자가 "지금 저장 안 됨"을 인지한 상태에서 닫음. console.warn으로 dev trace 남김. |
</threat_model>

<verification>
1. Task 1 acceptance_criteria 모든 항목 PASS.
2. Task 2 사용자 시각 검증 통과(approved 시그널).
3. 통합 진실문:
   - 정상 경로: setItem 정상 동작 시 banner는 항상 hidden. save 성공 후 storageError null.
   - 실패 경로: setItem throw 시 banner 노출 + 메모리 mutation 반영 + 앱 크래시 0.
   - retry 성공: banner 자동 숨김.
   - dismiss: banner 즉시 숨김 (storageError null 클리어).
4. 회귀 게이트:
   - `grep -rE "localStorage\." js/ | grep -v "js/storage.js"` -> 0
   - `grep -rE "innerHTML" js/` -> 0
   - `grep -rE "toISOString|\.toJSON\(|Date\.UTC" js/` -> 0
   - Phase 2/3 시나리오 모두 동작.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criteria 2개 모두 충족:
  1. 폴리시된 시각: refined typography(시스템 폰트 스택), subtle shadow(banner), smooth transitions(150ms 4곳), consistent spacing(spacing scale 토큰).
  2. localStorage 실패 시 크래시 0 + visible banner로 사용자에게 surface (silent fake-save 0).
- requirements STYL-01 (Plan 04-01에서 처리) + PERS-04 (Plan 04-02 + 04-03에서 처리) 둘 다 closed.
- 회귀 0: Phase 1/2/3 grep 게이트 모두 PASS, 시각 회귀 0.
- storage 경계 보존: js/storage.js 외부에 raw localStorage.* 0건.
</success_criteria>

<output>
완료 후 `.planning/phases/04-modern-polish-failure-ux/04-03-SUMMARY.md` 작성. Phase 4 종료 직전이므로 SUMMARY에 Phase 4 phase-gate 검증 매트릭스(자동 grep 게이트 + UAT 준비 항목) 포함.
</output>
