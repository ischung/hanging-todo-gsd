---
phase: 04-modern-polish-failure-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - js/storage.js
autonomous: true
requirements: [PERS-04]
tags: [html, storage, throw, banner-markup]

must_haves:
  truths:
    - "index.html의 <main id=\"app\"> 위쪽에 <aside id=\"storage-banner\" class=\"banner banner--danger\" hidden role=\"alert\"> 마크업이 존재한다"
    - "banner 마크업 내부에 메시지 <p>와 [data-action=\"retry\"], [data-action=\"dismiss\"] 버튼 2개가 있다"
    - "<head>에 <meta name=\"color-scheme\" content=\"light\"> 가 있다"
    - "js/storage.js의 save() 함수가 try/catch 없이 localStorage.setItem을 직접 호출하여 실패 시 throw 한다 (boolean 반환 제거)"
    - "js/storage.js의 load() 함수는 변경되지 않는다 (try/catch 보존)"
    - "storage 경계 패턴 보존: js/storage.js 외부에 raw localStorage.* 호출 0건"
  artifacts:
    - path: "index.html"
      provides: "storage-banner 마운트 노드 + retry/dismiss 버튼 + color-scheme meta"
      contains: "id=\"storage-banner\""
    - path: "js/storage.js"
      provides: "save() throw 시그널 (boolean 반환 제거)"
      contains: "localStorage.setItem(KEY"
  key_links:
    - from: "index.html #storage-banner"
      to: "css/styles.css .banner / .banner--danger"
      via: "class='banner banner--danger'"
      pattern: "class=\"banner banner--danger\""
    - from: "js/storage.js save()"
      to: "js/app.js commit() (Plan 04-03에서 catch)"
      via: "throw on setItem failure"
      pattern: "localStorage\\.setItem\\(KEY"
---

<objective>
PERS-04를 위한 HTML 마운트 노드와 storage throw 시그널을 도입한다. index.html에 banner 마크업과 color-scheme meta를 추가하고, js/storage.js의 save()를 boolean 반환에서 throw 형태로 전환한다. js/app.js 와이어링은 Plan 04-03에서 처리 — 본 plan은 마크업+throw까지만 자기 완결.

Purpose: PERS-04의 마크업 인프라 + storage 실패 시그널 채널 확보. 외부 모듈은 여전히 localStorage를 모름(storage 경계 보존), 실패 신호만 throw로 전달.

Output: index.html (수정), js/storage.js (수정).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md
@.planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@index.html
@js/storage.js

<interfaces>
<!-- 현재 storage.js 인터페이스 (변경 대상): -->

From js/storage.js (현재):
```javascript
export const KEY = 'hansung-todo:v1';
export function defaultState();             // 변경 없음
export function load();                     // 변경 없음 (try/catch 보존)
export function save(state): boolean;       // 변경 → save(state): void (실패 시 throw)
```

From js/app.js (현재 — 본 plan은 변경하지 않음, Plan 04-03가 처리):
- L71: `if (!save(state)) { console.warn(...) }` ← 04-03가 try/catch로 교체

<!-- 본 plan에서 save() 시그너처를 boolean→void+throw로 바꾸면 app.js의 L71이 항상 falsy를 만나 console.warn 폭주 -->
<!-- 따라서 04-02와 04-03는 같은 wave에 있되 files_modified가 겹치지 않으므로 병렬 실행 가능. 단 두 plan이 모두 commit된 후에야 앱이 정상 동작. -->
<!-- 실제로는 04-03이 04-02를 read해야 하므로 04-03의 wave를 2로 두는 것이 안전하다 — 04-03 plan에서 depends_on: [04-02]로 명시. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html에 banner 마크업 + color-scheme meta 추가</name>
  <read_first>
    - index.html (현재 13줄 — 마운트 노드 위치 확인)
    - .planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md (decision C banner 본문/버튼/위치 + decision F)
    - .planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md (`index.html (MODIFY)` 섹션 — verbatim 마크업)
  </read_first>
  <action>
index.html을 다음 내용으로 정확히 교체한다 (Phase 1~3 마크업 보존 + banner 노드 1개 신규 추가 + color-scheme meta 추가):

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="light" />
    <title>Hansung Todo</title>
    <link rel="stylesheet" href="./css/styles.css" />
  </head>
  <body>
    <!-- Phase 4 (PERS-04): 저장 실패 알림. 평소 hidden, 실패 시 app.js가 hidden 제거. -->
    <aside id="storage-banner" class="banner banner--danger" hidden role="alert">
      <p>저장에 실패했습니다. 시크릿 모드이거나 저장 공간이 부족할 수 있습니다.</p>
      <div class="banner__actions">
        <button type="button" data-action="retry">다시 시도</button>
        <button type="button" data-action="dismiss">닫기</button>
      </div>
    </aside>
    <main id="app"><!-- Phase 3: js/app.js mounts calendar + 날짜 헤더 + todo form/list 순서로 렌더 --></main>
    <script type="module" src="./js/app.js"></script>
  </body>
</html>
```

위치 규칙: `<aside id="storage-banner">` 는 `<main id="app">` **위쪽**에 둔다 (CONTEXT decision C). meta color-scheme은 viewport meta 다음, title 이전에 둔다.

**XSS 가드:** 모든 텍스트는 정적 HTML이며 사용자 입력 미반영. js/에서 banner 메시지를 동적으로 갈아끼우지 않는다(CONTEXT D — 단일 메시지 잠금).
  </action>
  <verify>
    <automated>grep -E 'id="storage-banner"' index.html &amp;&amp; grep -E 'class="banner banner--danger"' index.html &amp;&amp; grep -E 'role="alert"' index.html &amp;&amp; grep -E 'data-action="retry"' index.html &amp;&amp; grep -E 'data-action="dismiss"' index.html &amp;&amp; grep -E 'name="color-scheme"' index.html &amp;&amp; grep -E '<main id="app">' index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="storage-banner"' index.html` 가 1
    - `grep -c 'data-action="retry"' index.html` 가 1
    - `grep -c 'data-action="dismiss"' index.html` 가 1
    - `grep -c 'role="alert"' index.html` 가 1
    - `grep -c 'name="color-scheme"' index.html` 가 1
    - `grep -c '<main id="app">' index.html` 가 1 (보존)
    - banner는 `hidden` 속성 보유: `grep -E 'id="storage-banner"[^>]*\bhidden\b' index.html` 매치 1개
    - banner 위치 확인: `awk '/<aside id="storage-banner"/{a=NR} /<main id="app">/{m=NR} END{exit !(a&gt;0 &amp;&amp; m&gt;0 &amp;&amp; a&lt;m)}' index.html` 가 종료코드 0 (banner가 main보다 위에 있음)
    - 메시지 텍스트 정확: `grep -F "저장에 실패했습니다. 시크릿 모드이거나 저장 공간이 부족할 수 있습니다." index.html` 매치 1개
    - 버튼 라벨: `grep -F "다시 시도" index.html` 매치 1개, `grep -F "닫기" index.html` 매치 1개
  </acceptance_criteria>
  <done>banner 마크업 + retry/dismiss 버튼 + color-scheme meta 추가 완료. main 마운트 노드 보존, script tag 보존.</done>
</task>

<task type="auto">
  <name>Task 2: js/storage.js save()를 boolean 반환에서 throw로 전환</name>
  <read_first>
    - js/storage.js (현재 36줄)
    - .planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md (decision D — save() throw 시그널)
    - .planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md (`js/storage.js (MODIFY)` Phase 4 패턴 verbatim)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (Storage 경계 패턴 — 본 변경이 경계를 깨지 않는지 확인)
  </read_first>
  <action>
js/storage.js의 `save()` 함수(L28-35)를 다음으로 정확히 교체한다 (verbatim — PATTERNS Phase 4 패턴):

```javascript
export function save(state) {
  // Phase 4 (PERS-04): 실패 시 silent false 대신 throw — commit()에서 catch하여 banner 노출.
  // localStorage.* 노출은 여전히 이 모듈 내부에 갇혀 있다 (storage 경계 유지).
  localStorage.setItem(KEY, JSON.stringify(state));
}
```

**변경 범위 한정:**
- L1-2 헤더 주석 변경 금지.
- L4 `KEY` 변경 금지.
- L6-8 `defaultState()` 변경 금지.
- L10-26 `load()` 변경 금지 (try/catch 보존 — 손상 JSON 폴백은 PERS-04 범위 밖, CONTEXT D 명시).
- L28-35 `save()` 만 위 4줄(주석 2 + 본문 1 + signature) 형태로 교체. boolean 반환 제거, 외부 try 제거.

**경계 보존 진실문:** 본 task 직후 `grep -rE "localStorage\." js/ | grep -v "js/storage.js"` 결과는 0이어야 한다 (Phase 1/2/3 게이트 회귀 방지).
  </action>
  <verify>
    <automated>grep -E "^export function save\(state\)" js/storage.js &amp;&amp; grep "localStorage.setItem(KEY" js/storage.js &amp;&amp; ! grep -E "return true|return false" js/storage.js &amp;&amp; node -e "import('./js/storage.js').then(m =&gt; { const orig = globalThis.localStorage; globalThis.localStorage = { setItem(){ throw new Error('quota'); }, getItem(){ return null; } }; let threw=false; try { m.save({schemaVersion:1,todosByDate:{}}); } catch(e){ threw=true; } if(!threw) process.exit(1); console.log('throw OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function save" js/storage.js` 가 1
    - save() 본문에 `try` / `catch` 없음: `awk '/^export function save/,/^}/' js/storage.js | grep -cE "try\s*\{|catch" ` 가 0
    - save() 본문에 `return true` / `return false` 없음: `awk '/^export function save/,/^}/' js/storage.js | grep -cE "return\s+(true|false)"` 가 0
    - save() 본문에 `localStorage.setItem(KEY` 호출 1건 존재
    - load() 보존: `grep -c "^export function load" js/storage.js` 가 1, load() 내 try/catch 보존: `awk '/^export function load/,/^}/' js/storage.js | grep -c "catch"` 가 1 이상
    - 경계 진실문: `grep -rE "localStorage\." js/ | grep -v "js/storage.js" | wc -l` 가 0
    - Phase 1 게이트 보존: `grep -rE "toISOString|\.toJSON\(|Date\.UTC" js/` 결과 0건
    - Phase 4 (PERS-04) 헤더 주석 존재: `grep "Phase 4 (PERS-04)" js/storage.js` 매치 1개 이상
  </acceptance_criteria>
  <done>save()가 throw 시그널로 전환됨. load()/KEY/defaultState 보존. storage 경계 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → js/storage.js | save 실패가 throw로 표면화. 실패 메시지 자체는 외부에 노출되지 않음(catch site에서만 사용). |
| static HTML → user 시각 | banner 메시지는 정적 텍스트, 사용자 입력 미반영. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | banner 메시지 ("저장에 실패했습니다...") | mitigate | 메시지는 정적 HTML, 사용자 입력 미반영. JS에서 동적으로 textContent 갱신 안 함(CONTEXT D 단일 메시지 잠금). 위협 표면 0. |
| T-04-05 | Information Disclosure | save() throw가 노출하는 Error 객체 | accept | Error 메시지는 브라우저 표준 (QuotaExceededError 등). PII 미포함. console.warn은 dev 환경 노출이며 production user 영향 없음. Plan 04-03에서 banner 본문에 err 메시지를 textContent로 반영하지 않음(단일 메시지 잠금 — 정보 누출 0). |
| T-04-06 | Tampering | localStorage.setItem 호출 wrap 제거 | mitigate | save() 내부에서만 localStorage 접근 유지. 외부 모듈 직접 호출 0건 grep 게이트로 회귀 방지. |
</threat_model>

<verification>
1. Task 1/2의 모든 acceptance_criteria PASS.
2. 통합 진실문:
   - `grep -rE "localStorage\." js/ | grep -v "js/storage.js"` → 0
   - `grep -rE "innerHTML|outerHTML|insertAdjacentHTML" js/` → 0 (Phase 2 STYL-02 게이트 회귀 방지)
3. Node sanity test (Task 2 verify에 포함): mock localStorage가 throw하면 save()가 throw 전파 확인.
</verification>

<success_criteria>
- index.html에 storage-banner aside + retry/dismiss 버튼 + color-scheme meta 추가 완료.
- js/storage.js save()가 throw 형태로 전환됨, load()/KEY/defaultState 보존.
- storage 경계 (외부 raw localStorage.* 0건) 보존.
- Phase 1/2/3 grep 게이트 (UTC 토큰 0, innerHTML 0, localStorage 외부 0) 모두 PASS.
</success_criteria>

<output>
완료 후 `.planning/phases/04-modern-polish-failure-ux/04-02-SUMMARY.md` 작성.
</output>
