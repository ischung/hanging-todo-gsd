---
phase: 04-modern-polish-failure-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
autonomous: true
requirements: [STYL-01]
tags: [css, design-tokens, polish, banner-styles]

must_haves:
  truths:
    - "css/styles.css :root 블록에 spacing/shadow/transition/type/color 토큰이 모두 선언되어 있다"
    - "기존 하드코딩 색·간격 값이 모두 var(--token) 참조로 치환되어 시각 회귀 0"
    - "banner / banner--danger / banner__actions 룰이 존재하고 hidden 속성을 존중한다"
    - ".cal-cell, button, .todo-checkbox, .banner 셀렉터에 transition 속성이 추가되어 있다"
    - ".cal-grid / .todo-list / .banner 3곳에 box-shadow: var(--shadow-1)이 적용되어 있다 (CONTEXT A.2)"
    - "Phase 1/2/3 셀렉터(.todo-list .done .todo-text, .empty, .editing .todo-text, .cal-grid, .cal-cell-adjacent, .is-today, .is-selected)가 모두 보존된다"
    - "시스템 폰트 스택(-apple-system, Apple SD Gothic Neo, Pretendard, Malgun Gothic, system-ui)이 :root font-family에 선언되어 있다"
  artifacts:
    - path: "css/styles.css"
      provides: "design tokens at :root + 토큰 매핑된 기존 룰 + banner 룰 + transition + 3곳 shadow"
      contains: "--accent: #2563eb"
  key_links:
    - from: ".cal-cell.is-today"
      to: "var(--accent)"
      via: "border-color/color 토큰 참조"
      pattern: "border:\\s*2px\\s+solid\\s+var\\(--accent\\)"
    - from: ".banner--danger"
      to: "var(--danger)"
      via: "border-left 4px"
      pattern: "border-left:\\s*4px\\s+solid\\s+var\\(--danger\\)"
    - from: ".cal-grid / .todo-list / .banner"
      to: "var(--shadow-1)"
      via: "box-shadow 토큰 참조 (3곳, CONTEXT A.2)"
      pattern: "box-shadow:\\s*var\\(--shadow-1\\)"
---

<objective>
css/styles.css를 design token 기반으로 재구성한다. :root에 spacing/shadow/transition/type/color 토큰 + 시스템 폰트 스택을 선언하고, 기존 하드코딩 값(#2563eb, #1e40af, #999, #666, #ddd, #eee, #ccc, 0.25rem 등)을 토큰 참조로 치환한다. banner 컴포넌트 CSS와 transition 속성을 추가한다.

Purpose: STYL-01 — 폴리시된 시각(refined typography, subtle shadows, smooth transitions, consistent spacing)을 단일 stylesheet 내에서 마이크로 디테일로 달성. 마크업 변경 없음. 시각 회귀 0이 핵심 보장.

Output: css/styles.css (수정 — :root 교체 + 기존 룰 토큰 치환 + banner 블록 추가).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md
@.planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md
@.planning/phases/02-todo-crud/02-04-SUMMARY.md
@.planning/phases/03-calendar-integration/03-03-SUMMARY.md
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: :root design tokens 선언 + 시스템 폰트 스택</name>
  <read_first>
    - css/styles.css (전체 — 현재 :root placeholder L2-4 위치 확인)
    - .planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md (decision A 토큰 값, decision B 폰트 스택 — verbatim 복사)
    - .planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md (`:root 교체용 블록`)
  </read_first>
  <action>
css/styles.css 의 L1-4 (`/* Phase 1: ... */` 헤더 + `:root { /* placeholder */ }`)를 다음으로 정확히 교체한다 (verbatim — CONTEXT decision A/B 그대로):

```css
/* Phase 1: 정적 자산 구조(DEL-02) 충족용 골격. Phase 4 (STYL-01)에서 토큰화. */
:root {
  /* Phase 4 (STYL-01): design tokens. */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;

  --shadow-1: 0 1px 2px rgba(0,0,0,0.04), 0 1px 3px rgba(0,0,0,0.06);
  --transition: 150ms ease-out;

  --text-sm: 13px;
  --text-base: 15px;
  --text-lg: 18px;

  --accent: #2563eb;
  --accent-strong: #1e40af;
  --bg: #ffffff;
  --bg-muted: #f3f4f6;
  --text: #111827;
  --text-muted: #6b7280;
  --border: #e5e7eb;
  --danger: #dc2626;
  --danger-bg: #fef2f2;

  font-family:
    -apple-system, BlinkMacSystemFont,
    'Apple SD Gothic Neo',
    'Pretendard Variable', 'Pretendard',
    'Malgun Gothic',
    'Segoe UI', system-ui,
    sans-serif;
  color: var(--text);
  background: var(--bg);
  font-size: var(--text-base);
  line-height: 1.5;
}
```

기존 Phase 2/3 룰 블록 (L6-80)은 본 task에서 변경 금지 — Task 2에서 토큰 치환.
  </action>
  <verify>
    <automated>grep -E "^\s*--accent:\s*#2563eb;" css/styles.css &amp;&amp; grep -E "^\s*--shadow-1:" css/styles.css &amp;&amp; grep -E "^\s*--transition:\s*150ms" css/styles.css &amp;&amp; grep -E "Apple SD Gothic Neo" css/styles.css &amp;&amp; grep -E "Pretendard" css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\s*--space-[1-9]:" css/styles.css` 가 6 이상 (space-1/2/3/4/6/8)
    - `grep -c "^\s*--text-\(sm\|base\|lg\):" css/styles.css` 가 3
    - `grep -c "^\s*--\(accent\|accent-strong\|bg\|bg-muted\|text\|text-muted\|border\|danger\|danger-bg\):" css/styles.css` 가 9
    - `grep "font-family:" css/styles.css` 첫 매치 줄 다음 줄들에 `-apple-system`, `Apple SD Gothic Neo`, `Pretendard`, `Malgun Gothic`, `system-ui` 모두 존재
    - `grep -c "Phase 4 (STYL-01): design tokens" css/styles.css` 가 1
  </acceptance_criteria>
  <done>:root에 토큰 19개 + 시스템 폰트 스택 선언 완료, Phase 2/3 룰 블록 미변경.</done>
</task>

<task type="auto">
  <name>Task 2: 기존 하드코딩 값 토큰 치환 + transition 추가</name>
  <read_first>
    - css/styles.css (Task 1 직후 상태)
    - .planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md (`기존 하드코딩 → 토큰 매핑` 표 + transition 적용 위치)
    - .planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md (decision A — "기존 하드코딩 값과 일치하도록 매핑(시각 회귀 0)")
  </read_first>
  <action>
css/styles.css 기존 Phase 2 (L6-16)와 Phase 3 (L18-80) 블록의 하드코딩 색/간격을 다음 매핑대로 토큰 참조로 치환한다. **셀렉터·선언 순서·`text-decoration`·`pointer-events`·layout 속성(grid-template-columns, position, min-height 등)은 절대 변경 금지** — 시각 회귀 0이 절대 우선.

매핑 (CONTEXT/PATTERNS 결정 — `#999`/`#666`/`#ccc`는 PATTERNS의 "Notable Tension" 결정에 따라 **회귀 0 우선**으로 기존 값 유지하고, `#ddd`/`#eee`만 `var(--border)` 참조로 치환):

| 원본 라인 (의미) | 원본 값 | 치환 후 |
|---|---|---|
| `.todo-list .done .todo-text { color }` | `#999` | `#999` (유지 — 회귀 0 우선) |
| `.empty { color }` | `#999` | `#999` (유지) |
| `.cal-weekday { color }` | `#666` | `#666` (유지) |
| `.cal-cell { border }` | `1px solid #ddd` | `1px solid var(--border)` |
| `.cal-cell-adjacent { border }` | `1px solid #eee` | `1px solid var(--border)` |
| `.cal-cell-adjacent { color }` | `#ccc` | `#ccc` (유지) |
| `.cal-cell.is-today { border }` | `2px solid #2563eb` | `2px solid var(--accent)` |
| `.cal-cell.is-today { color }` | `#2563eb` | `var(--accent)` |
| `.cal-cell.is-selected { background }` | `#2563eb` | `var(--accent)` |
| `.cal-cell.is-today.is-selected { background }` | `#2563eb` | `var(--accent)` |
| `.cal-cell.is-today.is-selected { border }` | `2px solid #1e40af` | `2px solid var(--accent-strong)` |

(`.cal-cell.is-selected { color: #fff }` 및 `.cal-cell.is-today.is-selected { color: #fff }` 의 `#fff`는 토큰 미정의 — 그대로 유지.)

추가로, 다음 셀렉터 각각의 선언 블록에 `transition: var(--transition);` 라인 1개 추가 (CONTEXT decision A.3):
- `.cal-cell` (selector L40-49의 `}` 직전)
- `.cal-badge` 위는 추가 안 함. 추가 대상은 다음 4개로 한정:
  - `.cal-cell` { ... transition: var(--transition); }
  - 새로 추가할 `button { transition: var(--transition); }` 룰 (전역, 1줄짜리 보조 룰. 기존 button 룰이 없으므로 신규 추가 — 파일 말미 Phase 4 블록 헤더 아래에 둔다)
  - 새로 추가할 `.todo-checkbox { transition: var(--transition); }` (마찬가지로 파일 말미 Phase 4 블록)

`.banner { transition: opacity var(--transition); }`는 Task 3에서 banner 블록과 함께 추가.

파일 **말미**에 다음 헤더와 보조 룰을 append (Phase 1/2/3 append-only 패턴):

```css

/* Phase 4 (STYL-01): transition 적용 — hover/selected/toggle 부드러움. */
button {
  transition: var(--transition);
}
.todo-checkbox {
  transition: var(--transition);
}
```

`.cal-cell` 블록(L40-49) 자체에는 닫는 `}` 직전에 `  transition: var(--transition);` 한 줄을 삽입한다 — 이는 인라인 토큰 치환과 동일한 in-place 수정의 일부.
  </action>
  <verify>
    <automated>grep -c "var(--accent)" css/styles.css &amp;&amp; grep -c "var(--accent-strong)" css/styles.css &amp;&amp; grep -c "var(--border)" css/styles.css &amp;&amp; grep -c "transition: var(--transition)" css/styles.css &amp;&amp; grep -c "text-decoration: line-through" css/styles.css &amp;&amp; grep -c "pointer-events: none" css/styles.css &amp;&amp; grep -c "repeat(7, 1fr)" css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--accent)" css/styles.css` 가 4 이상 (.is-today border + .is-today color + .is-selected bg + .is-today.is-selected bg)
    - `grep -c "var(--accent-strong)" css/styles.css` 가 1 (.is-today.is-selected border)
    - `grep -c "var(--border)" css/styles.css` 가 2 이상 (.cal-cell + .cal-cell-adjacent)
    - `grep -c "transition: var(--transition)" css/styles.css` 가 3 이상 (.cal-cell + button + .todo-checkbox)
    - `grep -c "#2563eb" css/styles.css` 가 0 (모두 토큰으로 치환됨)
    - `grep -c "#1e40af" css/styles.css` 가 0
    - 회귀 가드 (모두 보존):
      - `grep -c "text-decoration: line-through" css/styles.css` 가 1
      - `grep -c "pointer-events: none" css/styles.css` 가 1
      - `grep -c "repeat(7, 1fr)" css/styles.css` 가 1
      - `grep -c "\.cal-cell\.is-today\.is-selected" css/styles.css` 가 1
      - `grep -c "\.editing \.todo-text" css/styles.css` 가 1
  </acceptance_criteria>
  <done>모든 accent/accent-strong/border 하드코딩이 토큰 참조로 치환됨. 기존 셀렉터 9개와 핵심 layout 선언(grid-template-columns/pointer-events/text-decoration) 모두 보존. transition 3곳 추가.</done>
</task>

<task type="auto">
  <name>Task 3: banner CSS 룰 append + 캘린더/리스트/banner 3곳 subtle shadow 적용</name>
  <read_first>
    - css/styles.css (Task 2 직후 상태 — 파일 말미 Phase 4 transition 블록 위치 확인)
    - .planning/phases/04-modern-polish-failure-ux/04-CONTEXT.md (decision A.2 — shadow는 캘린더/리스트/banner 3곳에 적용; decision C — banner 마크업/색/구조)
    - .planning/phases/04-modern-polish-failure-ux/04-PATTERNS.md (`Phase 4 추가 룰 (banner)` + `색맹 안전 패턴`)
    - js/calendar.js L83 (`.cal-grid` 컨테이너) / js/todos.js L77 (`.todo-list` ul 컨테이너) — shadow 적용 셀렉터 verbatim 확인 완료
  </read_first>
  <action>
css/styles.css **파일 말미**(Task 2의 `/* Phase 4 (STYL-01): transition ... */` 블록 다음)에 다음 두 블록을 순서대로 append한다 (verbatim — CONTEXT A.2 "shadow는 캘린더 컨테이너 / todo list / banner에만 적용" 3곳 잠금 결정 그대로 반영):

**(1) shadow elevation 블록 (캘린더 + 리스트 컨테이너)** — STYL-01 "subtle shadows" 진실문은 banner 단독으로는 평상시 hidden이라 충족 불가. CONTEXT A.2가 명시한 3곳 중 캘린더(`.cal-grid`)·리스트(`.todo-list`)는 페이지 첫 로드부터 보이는 표면이므로 polish 인지가 여기서 발생한다.

```css

/* Phase 4 (STYL-01): subtle elevation — CONTEXT A.2 잠금 (캘린더/리스트/banner 3곳 한정).
   shadow는 elevation only — outer box 영역 변화 0 (margin/padding/border-width 미변경) → 시각 회귀 0 보존. */
.cal-grid {
  box-shadow: var(--shadow-1);
}
.todo-list {
  box-shadow: var(--shadow-1);
}
```

**(2) banner 룰 블록** — PATTERNS의 banner 룰 그대로 + banner도 `--shadow-1` 1단계 (3곳 중 세 번째):

```css

/* Phase 4 (PERS-04): storage failure banner. 색맹 안전 3중 신호(배경 + 좌측 막대 + 텍스트). */
.banner {
  display: block;
  padding: var(--space-3) var(--space-4);
  margin-bottom: var(--space-4);
  border-radius: 4px;
  box-shadow: var(--shadow-1);
  transition: opacity var(--transition);
}
.banner[hidden] {
  display: none;
}
.banner--danger {
  background: var(--danger-bg);
  color: var(--text);
  border-left: 4px solid var(--danger);
}
.banner__actions {
  display: flex;
  gap: var(--space-2);
  margin-top: var(--space-2);
}
```

**Trade-off 노트 (회귀 0 보존 근거):** box-shadow는 element의 layout box(width/height/border/margin) 외부에 페인트되는 elevation only 속성이다. 따라서 `.cal-grid`·`.todo-list`에 추가해도 grid 셀 위치·todo 행 높이·요일 헤더 정렬 모두 불변. CONTEXT A "회귀 0" 원칙은 layout 변동 금지를 의미하며 shadow 추가는 이 원칙을 위반하지 않는다.

마크업 셀렉터는 코드 verbatim 확인 완료: `.cal-grid` (js/calendar.js L83 `grid.className = 'cal-grid'`), `.todo-list` (js/todos.js L77 `ul.className = 'todo-list'`).

마크업·storage throw 도입은 Plan 04-02에서, app.js 와이어링은 Plan 04-03에서 처리. 본 plan은 CSS만 자기 완결.
  </action>
  <verify>
    <automated>grep -E "^\.banner\s*\{" css/styles.css &amp;&amp; grep -E "^\.banner--danger" css/styles.css &amp;&amp; grep -E "^\.banner__actions" css/styles.css &amp;&amp; grep "border-left: 4px solid var(--danger)" css/styles.css &amp;&amp; grep "\.banner\[hidden\]" css/styles.css &amp;&amp; [ "$(grep -c 'box-shadow: var(--shadow-1)' css/styles.css)" -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.banner" css/styles.css` 가 4 이상 (.banner / .banner[hidden] / .banner--danger / .banner__actions)
    - `grep -c "border-left: 4px solid var(--danger)" css/styles.css` 가 1
    - `grep -c "box-shadow: var(--shadow-1)" css/styles.css` 가 **3 이상** (`.cal-grid` + `.todo-list` + `.banner` — CONTEXT A.2 3곳 잠금)
    - `grep -E "^\.cal-grid\s*\{" css/styles.css` 매치 1개 이상 (Phase 4 append 블록의 신규 `.cal-grid` 룰. 기존 Phase 3 `.cal-weekdays, .cal-grid` 멀티 셀렉터 룰은 별개로 보존)
    - `grep -E "^\.todo-list\s*\{" css/styles.css` 매치 1개 이상 (Phase 4 append 블록의 신규 `.todo-list` 룰)
    - `grep -c "Phase 4 (STYL-01): subtle elevation" css/styles.css` 가 1
    - `grep -c "Phase 4 (PERS-04): storage failure banner" css/styles.css` 가 1
    - 색맹 안전 진실문: `.banner--danger` 룰에 background + border-left + color 3가지 모두 선언
    - 회귀 가드: Task 2의 acceptance 모두 여전히 PASS (특히 `repeat(7, 1fr)`, `text-decoration: line-through`, `pointer-events: none`, `.cal-cell.is-today.is-selected` 모두 보존)
  </acceptance_criteria>
  <done>banner 4룰 + 캘린더/리스트 shadow 2룰 append. shadow는 `.cal-grid` / `.todo-list` / `.banner` 3곳에 모두 `var(--shadow-1)`로 적용 (CONTEXT A.2 잠금 충족). danger color + transition 모두 토큰 참조. layout 속성 미변경 → 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet → user 시각 | 사용자가 직접 입력한 텍스트가 아니므로 정적 CSS는 신뢰 boundary 미통과. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | css/styles.css 토큰 값 | accept | 정적 CSS, 사용자 입력 미반영. CDN 의존 없음(시스템 폰트 스택만) — 외부 폰트 fetch 0건. |
| T-04-02 | Information Disclosure | banner 메시지 텍스트 | mitigate | banner 본문은 정적 HTML(Plan 04-02), CSS는 마크업에 텍스트 주입 안 함(content: 미사용). 본 plan 범위 내 surface 없음. |
| T-04-03 | Denial of Service | shadow/transition 성능 | accept | shadow는 3곳(cal-grid/todo-list/banner)에 1단계 elevation만 적용, transition 150ms ease-out — 페인트 비용 무시 가능. |
</threat_model>

<verification>
1. Task 1/2/3의 모든 acceptance_criteria PASS.
2. 시각 회귀 가드: Phase 1/2/3 셀렉터 모두 보존 (grep으로 자동 확인).
3. CDN 의존 0: `grep -E "@import|url\(http" css/styles.css` → 0.
4. !important 미도입: `grep -c "!important" css/styles.css` 가 0 (이전 phase 정책 유지).
5. CONTEXT A.2 충족: `grep -c "box-shadow: var(--shadow-1)" css/styles.css` 가 3 이상.
</verification>

<success_criteria>
- :root에 design tokens 19개 + 시스템 폰트 스택 선언.
- 기존 accent/accent-strong/border 하드코딩이 모두 토큰 참조로 치환되어 시각 회귀 0.
- banner / banner--danger / banner__actions 룰 존재.
- transition 3곳(.cal-cell, button, .todo-checkbox) + banner 1곳 적용.
- box-shadow `--shadow-1`이 `.cal-grid` / `.todo-list` / `.banner` 3곳에 적용 (CONTEXT A.2 잠금 충족, "subtle shadows" 첫인상 인지 surface 확보).
- Phase 1/2/3 셀렉터/layout 선언 모두 보존.
</success_criteria>

<output>
완료 후 `.planning/phases/04-modern-polish-failure-ux/04-01-SUMMARY.md` 작성.
</output>
</content>
</invoke>