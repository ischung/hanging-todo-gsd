---
phase: 02-todo-crud
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - css/styles.css
autonomous: false
requirements: [TODO-03, STYL-02]
tags: [css, minimal-state-styles, phase-gate, xss-grep-gate]

must_haves:
  truths:
    - "사용자는 입력 + Enter/추가 버튼 → 즉시 리스트 반영, input clear, focus 유지"
    - "사용자는 체크박스로 완료 토글 가능 (시각적 strikethrough/회색 구분)"
    - "사용자는 텍스트 인라인 편집 가능 (Enter/blur 저장, Esc 취소, 빈/공백만 입력은 거부 + 원래 텍스트 유지)"
    - "사용자는 즉시 삭제 가능 (확인 다이얼로그 없음)"
    - "0건 일 때 '아직 할 일이 없습니다.' 메시지 노출"
    - "reload 후 모든 todo가 createdAt 오름차순 안정 정렬로 동일하게 복원됨"
    - "<img src=x onerror=alert(1)> 입력 시 alert 발생 없이 literal text로 표시됨"
    - "한국어 IME 입력으로 '공부하기' 추가/편집 시 잘린 자모 없이 그대로 저장됨"
    - "Phase 1 진실문 유지: js/ 트리에서 toISOString/toJSON/Date.UTC 0건"
    - "storage 경계 유지: js/storage.js 외부에서 raw localStorage.* 호출 0건"
    - "XSS 사전 방어 진실문 유지: js/ 트리에서 innerHTML/outerHTML/insertAdjacentHTML 0건"
  artifacts:
    - path: "js/todos.js"
      provides: "5 immutable helpers + renderTodoList/renderTodoItem"
      contains: "export function renderTodoList"
    - path: "js/app.js"
      provides: "TODAY 캡처 + DOM 마운트 + 위임 4종 + 편집 라이프사이클"
      contains: "listContainer.addEventListener('focusout'"
    - path: "css/styles.css"
      provides: "최소 상태 스타일 3룰 (.done .todo-text / .empty / .editing .todo-text)"
      contains: "text-decoration: line-through"
    - path: "index.html"
      provides: "Phase 1과 동일 (변경 없음 또는 <title>만 미세 조정)"
      contains: "<main id=\"app\">"
  key_links:
    - from: "사용자 input → addTodo"
      to: "save(state) → localStorage"
      via: "form submit → commit() → save()"
      pattern: "form\\.addEventListener\\('submit'"
    - from: "체크박스 change → toggleTodo"
      to: "li.done CSS"
      via: "renderTodoItem이 t.done에 따라 classList.add('done')"
      pattern: "classList\\.add\\(['\"]done['\"]\\)|classList\\.toggle"
    - from: "편집 input commit"
      to: "editingId guard"
      via: "commitEdit 첫 줄 editingId=null"
      pattern: "const id = editingId;\\s*editingId = null"
---

<objective>
미니멀 CSS 3룰만 추가(`.done .todo-text` strikethrough/회색, `.empty` 회색, `.editing .todo-text` display:none)하고, Phase 2 전체 검증 체크리스트(12 시나리오 + 4 grep gate)를 사용자가 함께 실행하여 phase-level must_haves를 모두 충족시킨다.

Purpose: TODO-03의 "시각 구분" 책임을 CSS로 마무리하고, STYL-02의 진실문(`innerHTML` 0건)을 phase 종료 시점에 다시 한 번 grep gate로 잠근다. Phase 4 STYL-01 본격 폴리시는 절대 끌어오지 않는다.

Output: `css/styles.css` 수정 (Phase 1 4줄 보존 + 3룰 추가) + Phase 2 종료 검증 통과.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-todo-crud/02-CONTEXT.md
@.planning/phases/02-todo-crud/02-RESEARCH.md
@.planning/phases/02-todo-crud/02-PATTERNS.md
@.planning/phases/02-todo-crud/02-01-SUMMARY.md
@.planning/phases/02-todo-crud/02-02-SUMMARY.md
@.planning/phases/02-todo-crud/02-03-SUMMARY.md
</execution_context>

<context>
@css/styles.css
@js/app.js
@js/todos.js
@index.html
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: css/styles.css — 최소 상태 스타일 3룰 추가 (Phase 1 4줄 보존)</name>
  <files>css/styles.css</files>
  <read_first>
    - .planning/phases/02-todo-crud/02-CONTEXT.md (decisions: 토글 UX 시각 구분, 빈 상태, 마크업 추가 위치)
    - .planning/phases/02-todo-crud/02-PATTERNS.md (`css/styles.css` 섹션 lines 239-269)
    - .planning/phases/02-todo-crud/02-RESEARCH.md (Phase 4 폴리시 범위 보존: deferred_ideas, STYL-01 transfer)
    - css/styles.css (Phase 1 4줄 현재 상태)
  </read_first>
  <behavior>
    - `<li class="done">`의 `<span class="todo-text">`는 strikethrough + 회색.
    - `<p class="empty">`는 회색.
    - `<li class="editing">`의 `<span class="todo-text">`는 display:none (input이 같은 자리에 노출되도록).
    - Phase 1의 `:root` placeholder 주석과 빈 블록은 그대로 유지.
  </behavior>
  <action>
**Phase 1 4줄을 절대 수정하지 말 것.** 기존 마지막 줄(`}`) 다음에 아래 블록만 append:

```css

/* Phase 2: todo 행 상태 시각 구분 (미니멀). 본격 폴리시는 Phase 4 (STYL-01). */
.todo-list .done .todo-text {
  text-decoration: line-through;
  color: #999;
}
.empty {
  color: #999;
}
.editing .todo-text {
  display: none;
}
```

**금지 사항** (실패 조건):
- 디자인 토큰(`--color-*`, `--space-*`) 추가 0건 (Phase 4 STYL-01 영역).
- `transition` / `animation` / `box-shadow` / `@keyframes` / `@import` 추가 0건.
- 폰트 임포트(`@font-face`, `font-family` 외부) 0건.
- 새 CSS 변수 선언(`--xxx`) 0건 — `:root`는 빈 placeholder 유지.
- 위 3개 셀렉터 외 어떤 룰도 추가 금지 (예: `body`, `main`, `input`, `button` 글로벌 스타일 금지).
- `!important` 0건.
  </action>
  <verify>
    <automated>
      cd /Users/insang/Documents/강의/소프트웨어공학/2026/대면수업용/sdd/gsd/hansung-todo-gsd &amp;&amp; \
      head -4 css/styles.css | diff - <(printf "/* Phase 1: 정적 자산 구조(DEL-02) 충족용 골격. 본격 스타일은 Phase 4 (STYL-01). */\n:root {\n  /* design tokens placeholder — Phase 4에서 채움 */\n}\n") &amp;&amp; \
      grep -q "text-decoration: line-through" css/styles.css &amp;&amp; \
      grep -q "\.empty" css/styles.css &amp;&amp; \
      grep -q "\.editing \.todo-text" css/styles.css &amp;&amp; \
      grep -q "\.todo-list \.done \.todo-text" css/styles.css &amp;&amp; \
      ! grep -E "@import|@font-face|@keyframes|transition\s*:|animation\s*:|box-shadow\s*:|!important" css/styles.css &amp;&amp; \
      ! grep -E "^\s*--[a-zA-Z]" css/styles.css &amp;&amp; \
      echo PASS
    </automated>
  </verify>
  <done>
    - css/styles.css 처음 4줄 byte-equal Phase 1.
    - 3룰 정확히 추가됨.
    - 디자인 토큰 / 애니메이션 / 그림자 / 폰트 임포트 / !important 0건.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 phase-gate 검증 — 12 시나리오 + grep gate 통과 확인</name>
  <what-built>
    - Plan 02-01: js/todos.js (5 helpers + renderTodoList/renderTodoItem).
    - Plan 02-02: js/app.js (TODAY/DOM 마운트/commit/render/form submit/최초 render).
    - Plan 02-03: js/app.js (위임 4종 + 편집 라이프사이클).
    - Plan 02-04 Task 1: css/styles.css (.done / .empty / .editing 3룰).
  </what-built>
  <how-to-verify>

**자동 grep gate (executor가 먼저 실행):**

```bash
cd /Users/insang/Documents/강의/소프트웨어공학/2026/대면수업용/sdd/gsd/hansung-todo-gsd
# Gate 1: innerHTML/outerHTML/insertAdjacentHTML 0건
grep -rE "innerHTML|outerHTML|insertAdjacentHTML" js/ && echo FAIL || echo PASS_1
# Gate 2: js/ 트리에서 UTC 토큰 0건 (Phase 1 진실문 유지)
grep -rE "toISOString|\.toJSON\(|Date\.UTC" js/ && echo FAIL || echo PASS_2
# Gate 3: storage 외부에서 raw localStorage 0건
grep -rE "localStorage\." js/ | grep -v "js/storage.js" && echo FAIL || echo PASS_3
# Gate 4: js/todos.js는 dateKey/KEY 미import
grep -E "dateKey\.js|from.*storage.*KEY" js/todos.js && echo FAIL || echo PASS_4
```

**브라우저 검증 (사용자가 함께 수행):**

1. `python3 -m http.server 8000` 후 `http://localhost:8000` 접속. 콘솔 에러 0건.
2. **빈 상태:** 첫 진입 시 "아직 할 일이 없습니다." 노출 (TODO-07).
3. **추가 (Enter):** "공부" + Enter → 즉시 리스트 노출, input clear + focus 유지 (TODO-01).
4. **추가 (버튼):** "운동" 입력 후 추가 버튼 클릭 → 리스트에 추가됨.
5. **빈/공백만 추가:** 빈 input + Enter → 무반응. 공백만 + Enter → 무반응.
6. **토글:** "운동" 체크박스 클릭 → strikethrough + 회색 적용 (TODO-03).
7. **인라인 편집 — Enter 저장:** "공부" 텍스트 클릭 → input → "공부하기" → Enter → 저장.
8. **인라인 편집 — blur 저장:** "운동" ✎ → input → "운동하기" → 다른 곳 클릭 → 저장.
9. **인라인 편집 — Esc 취소:** "공부하기" → input → "포기" → Esc → "공부하기" 그대로 (TODO-04).
10. **인라인 편집 — 빈 거부:** 텍스트 클릭 → input → 전부 지움 → Enter → 원래 텍스트 유지.
11. **삭제:** 🗑 클릭 → confirm 없이 즉시 사라짐 (TODO-05).
12. **정렬:** 빠르게 4개 추가 → 추가 순서대로 위→아래 노출 (TODO-06).
13. **영속화:** 위 모든 작업 후 F5 → 동일 상태 복원 (PERS-03).
14. **XSS:** 입력란에 `<img src=x onerror=alert(1)>` + Enter → alert 발생 없음, literal text로 표시. DevTools Elements에서 `<img>` 노드 0개 (STYL-02).
15. **한국어 IME:** "공부하기" IME 입력 → 마지막 Enter 1회로 정확히 저장. "공부하긱" 같은 깨짐 0건.
16. **focusout 재진입 회귀:** 편집 input에 입력 후 다른 행 텍스트 클릭 → 첫 행 commit, 새 행 startEdit, localStorage 갱신 1회만 (DevTools Application 탭에서 키 값 변화 횟수 추적).

**모두 PASS 시 phase 완료. 한 항목이라도 FAIL이면 issue를 명시하고 plan으로 회귀.**
  </how-to-verify>
  <resume-signal>모든 grep gate 4종 + 16개 브라우저 시나리오 통과 시 "approved" 입력. 실패 시 어느 항목이 어떻게 실패했는지 기재.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (이 plan은 신규 trust boundary 도입 0건 — CSS만 추가) | 사용자 입력 → DOM trust boundary는 02-01/02-03에서 mitigate됨. 본 plan은 phase-level grep gate로 진실문 잠금 재확인. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering (regression) | js/ 트리 전체 | mitigate | phase-gate 시점에 grep gate 4종으로 진실문 재확인. innerHTML / UTC 토큰 / raw localStorage / dateKey-leak 모두 0건 검증. |
</threat_model>

<verification>
**Grep gate (자동, phase 종료 시점):**
- `grep -rE "innerHTML|outerHTML|insertAdjacentHTML" js/` → 0건.
- `grep -rE "toISOString|\.toJSON\(|Date\.UTC" js/` → 0건.
- `grep -rE "localStorage\." js/ | grep -v "js/storage.js"` → 0건.
- `grep -E "dateKey\.js" js/todos.js` → 0건 (todos.js는 dateKey import 금지).

**Phase-gate 사용자 검증:** 위 task 2의 16개 시나리오 모두 PASS.

**Phase 1 진실문 회귀 방지:**
- `head -12 js/app.js` byte-equal Phase 1 (12줄).
- `head -4 css/styles.css` byte-equal Phase 1 (4줄).
- index.html 변경 0 (또는 `<title>`만 미세 조정 — 본 plan은 변경 안 함).
</verification>

<success_criteria>
- [ ] css/styles.css에 3룰 추가, Phase 1 4줄 보존.
- [ ] grep gate 4종 모두 PASS.
- [ ] 사용자 16개 시나리오 모두 PASS (특히 XSS / IME / focusout 재진입 / reload 영속화).
- [ ] phase-level must_haves(11 truths + 4 artifacts + 3 key_links) 모두 검증됨.
- [ ] STYL-01(본격 폴리시) / PERS-04(토스트) / Phase 3(캘린더) 절대 미리 도입 안 됨.
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-crud/02-04-SUMMARY.md` documenting:
- Modified file: css/styles.css (Phase 1 4줄 보존 + 3룰).
- Phase 2 phase-gate 결과 (grep 4 + 시나리오 16개 매트릭스).
- Phase 1 진실문 회귀 검증 결과.
- Phase 3 진입 준비 상태: TODAY → selectedDate 변경 지점, listContainer 위치 재배치 후보, window.* dev hook 정리 후보.
- Open todos 인계: window.* dev hook 정리(STATE 기록), <main> 마운트 구조 Phase 3 재배치.
</output>
