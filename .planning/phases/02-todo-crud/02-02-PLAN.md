---
phase: 02-todo-crud
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - js/app.js
autonomous: true
requirements: [TODO-01, TODO-07, PERS-03]
tags: [vanilla-js, esm, bootstrap, dom-mount, form-submit, ime-guard]

must_haves:
  truths:
    - "TODAY = dateKey() captured once at module load"
    - "<main id=\"app\"> is dynamically mounted with form + listContainer (1회만)"
    - "추가 폼은 <form>+submit (별도 keydown Enter 리스너 0건 — IME 자동 우회)"
    - "addTodo 후 input.value='' + input.focus() (연속 입력)"
    - "save() 실패 시 console.warn('[hansung-todo] save failed; mutation kept in memory') 출력, in-memory state 유지"
    - "최초 렌더 시 todos 0건 → '아직 할 일이 없습니다.' empty-state 노출 (TODO-07)"
    - "Phase 1 window.* dev 노출 13줄 보존 (append-only 변경)"
  artifacts:
    - path: "js/app.js"
      provides: "Phase 2 부트스트랩 + 추가 폼 핸들러 + 최초 렌더"
      contains: "const TODAY = dateKey()"
  key_links:
    - from: "js/app.js (form submit)"
      to: "todos.js addTodo"
      via: "commit(addTodo(state, TODAY, text))"
      pattern: "addTodo\\(state, TODAY"
    - from: "js/app.js commit()"
      to: "storage.js save"
      via: "if (!save(state))"
      pattern: "if\\s*\\(\\s*!\\s*save\\("
---

<objective>
`js/app.js`를 append-only로 확장하여: TODAY 캡처 → DOM 마운트(폼 + 리스트 컨테이너 1회) → 최초 렌더 → 추가 폼 submit 핸들러 와이어링까지 수행한다. 이 plan은 토글/편집/삭제 핸들러를 다루지 **않는다** (02-03에서 추가).

Purpose: 사용자가 페이지를 열자마자 폼이 보이고 빈 상태 메시지가 노출되며, 추가 → Enter/버튼 → 즉시 리스트 반영 + reload 후 유지가 동작한다 (Success Criteria #1, #4).

Output: `js/app.js` 수정 (Phase 1 13줄 보존 + 부트스트랩/폼/commit/render 추가).
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-todo-crud/02-CONTEXT.md
@.planning/phases/02-todo-crud/02-RESEARCH.md
@.planning/phases/02-todo-crud/02-PATTERNS.md
@.planning/phases/02-todo-crud/02-01-SUMMARY.md
</execution_context>

<context>
@js/app.js
@js/todos.js
@js/storage.js
@js/dateKey.js
@index.html

<interfaces>
<!-- 02-01에서 확정된 todos.js exports. -->

From js/todos.js:
```js
export function getTodosForDate(state, key): Todo[];
export function addTodo(state, key, text): State;          // text.trim()=='' → state 그대로
export function toggleTodo(state, key, id): State;
export function editTodo(state, key, id, text): State;     // text.trim()=='' → state 그대로
export function removeTodo(state, key, id): State;
export function renderTodoList(container, todos, editingId): void;
```

From js/storage.js (Phase 1):
```js
export function load(): State;
export function save(state): boolean;
```

From js/dateKey.js (Phase 1):
```js
export function dateKey(d?: Date): string;  // 'YYYY-MM-DD' local-time
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: js/app.js append — TODAY/state/editingId + DOM 마운트 + commit/render + form submit</name>
  <files>js/app.js</files>
  <read_first>
    - .planning/phases/02-todo-crud/02-CONTEXT.md (decisions: app.js 변경 범위, 마크업 추가 위치, save() 실패 처리)
    - .planning/phases/02-todo-crud/02-RESEARCH.md (§Pattern 2 lines 286-384, §"<form> 기반" lines 555-565, Pitfall 1)
    - .planning/phases/02-todo-crud/02-PATTERNS.md (`js/app.js` 섹션 lines 86-235; 특히 lines 91-105 "기존 상태 보존")
    - .planning/phases/02-todo-crud/02-01-SUMMARY.md (todos.js 인터페이스 확정)
    - js/app.js 현재 상태 (Phase 1 13줄)
    - js/storage.js, js/dateKey.js (시그니처 재확인)
  </read_first>
  <behavior>
    - 페이지 로드 시 콘솔 부트 로그(`[hansung-todo] foundation loaded`) 그대로 출력.
    - `<main id="app">` 안에 `<form>` (input + 추가 버튼) + 별도 `<div>` listContainer 2개 노드가 동적으로 추가됨.
    - 최초 진입 시 listContainer에 `<p class="empty">아직 할 일이 없습니다.</p>` 노출 (load() → 비어있을 때).
    - 입력란에 텍스트 + Enter → 즉시 `<ul><li>...</li></ul>`로 전환, input.value='' + input.focus(). 한국어 입력 도중 Enter는 IME에 의해 submit으로 변환되지 않음 (`<form>` 기본 동작).
    - 입력란이 빈 문자열/공백만일 때 Enter/버튼 → submit 가드로 무반응.
    - 추가 후 reload → 동일 todo가 그대로 보임.
    - save() 실패(개발자가 quota를 강제로 채워 시뮬레이션) 시 console.warn 1회 + UI는 정상 갱신.
  </behavior>
  <action>
**Phase 1 13줄을 절대 수정하지 말 것.** import 라인, window.* 노출, console.info 부트 로그 모두 그대로 유지(append-only).

기존 마지막 줄(`console.info('[hansung-todo] foundation loaded', ...);`) 다음에 아래를 순서대로 추가:

1) **추가 import** (multi-line, 02-01과 동일 형태):
```js
import {
  getTodosForDate, addTodo, renderTodoList,
} from './todos.js';
```
(이 plan은 toggle/edit/remove를 호출하지 않으므로 import도 추가하지 않는다 — 02-03에서 import 라인에 추가됨.)

2) **모듈 스코프 변수 3종** (PATTERNS lines 117-124):
```js
const TODAY = dateKey();
let state = load();
let editingId = null; // transient UI state — store 외부 (02-03에서 사용)
```

3) **DOM 마운트 — buildForm/buildListContainer** (PATTERNS lines 126-135, RESEARCH lines 297-302):
- `buildForm()` 내부: `<form>` + `<input type="text" id="todo-input" placeholder="할 일을 입력하세요" autocomplete="off">` + `<button type="submit" id="todo-add">추가</button>` 3노드를 createElement + append로 빌드. innerHTML 금지.
- `buildListContainer()` 내부: `<div id="todo-list">` 1노드를 createElement로 빌드.
- `const root = document.getElementById('app'); root.replaceChildren(form, listContainer);` — 한 번에 마운트.

4) **commit / render 헬퍼** (PATTERNS lines 137-150, RESEARCH lines 304-315):
```js
function commit(nextState) {
  state = nextState;
  if (!save(state)) {
    console.warn('[hansung-todo] save failed; mutation kept in memory');
  }
  render();
}
function render() {
  const todos = getTodosForDate(state, TODAY);
  renderTodoList(listContainer, todos, editingId);
}
```

5) **추가 폼 submit 핸들러** (PATTERNS lines 218-227, RESEARCH lines 555-565):
```js
form.addEventListener('submit', (e) => {
  e.preventDefault();
  const input = form.querySelector('input');
  if (!input.value.trim()) return;
  commit(addTodo(state, TODAY, input.value));
  input.value = '';
  input.focus();
});
```

6) **최초 렌더 호출** (파일 마지막 줄):
```js
render();
```

**금지 사항** (실패 조건):
- Phase 1 라인 1-12 수정 0건 (line by line diff에서 추가만 있어야 함).
- `<form>` 외부에 별도 `keydown Enter` 핸들러 추가 0건 (Pitfall 1 — IME 가드는 `<form>+submit`이 자동으로 처리).
- `localStorage.` 직접 호출 0건 (storage 경계 유지).
- `innerHTML` / `outerHTML` 좌변 0건. DOM 빌드는 createElement + append + replaceChildren만.
- `toISOString` / `Date.UTC` / `toJSON` 0건.
- 02-03에서 다룰 toggle/edit/remove 핸들러를 미리 끌어오지 말 것 (scope creep 금지).
  </action>
  <verify>
    <automated>
      cd /Users/insang/Documents/강의/소프트웨어공학/2026/대면수업용/sdd/gsd/hansung-todo-gsd &amp;&amp; \
      head -12 js/app.js | diff - <(printf "import { dateKey } from './dateKey.js';\nimport { KEY, defaultState, load, save } from './storage.js';\n\n// Phase 1: UI 없음. 콘솔에서 검증 체크리스트 2/3번 실행을 가능하게 만드는 dev 노출.\n// Phase 2 도입 시 재검토 (제거 또는 축소).\nwindow.dateKey = dateKey;\nwindow.defaultState = defaultState;\nwindow.load = load;\nwindow.save = save;\nwindow.STORAGE_KEY = KEY;\n\nconsole.info('[hansung-todo] foundation loaded', { today: dateKey() });\n") &amp;&amp; \
      grep -q "from './todos.js'" js/app.js &amp;&amp; \
      grep -q "const TODAY = dateKey()" js/app.js &amp;&amp; \
      grep -q "let state = load()" js/app.js &amp;&amp; \
      grep -q "let editingId = null" js/app.js &amp;&amp; \
      grep -q "root.replaceChildren(form, listContainer)" js/app.js &amp;&amp; \
      grep -q "if (!save(state))" js/app.js &amp;&amp; \
      grep -q "save failed; mutation kept in memory" js/app.js &amp;&amp; \
      grep -q "form.addEventListener('submit'" js/app.js &amp;&amp; \
      grep -q "input.focus()" js/app.js &amp;&amp; \
      ! grep -E "innerHTML\s*=|outerHTML\s*=" js/app.js &amp;&amp; \
      ! grep -E "localStorage\." js/app.js &amp;&amp; \
      ! grep -E "toISOString|toJSON|Date\.UTC" js/app.js &amp;&amp; \
      ! grep -E "addEventListener\(['\"]keydown['\"]" js/app.js &amp;&amp; \
      grep -cE "^render\(\);" js/app.js | grep -q "^[[:space:]]*1$" &amp;&amp; \
      echo PASS
    </automated>
  </verify>
  <done>
    - js/app.js의 처음 12줄이 Phase 1과 byte-for-byte 동일.
    - 추가된 코드: todos.js import / TODAY / state / editingId / DOM 마운트 / commit+render / form submit / 최초 render() 호출.
    - keydown Enter 핸들러 0건. raw localStorage 0건. innerHTML 0건. UTC 토큰 0건.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `<input id="todo-input">` 사용자 입력 → `addTodo` | 사용자 텍스트가 처음 state에 진입하는 지점. |
| `save()` 실패 → in-memory state | 영속화가 거부되어도 UI는 갱신되는 비대칭 지점. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | form submit handler | mitigate | `<form>` + `submit` 사용으로 IME 도중 Enter는 submit으로 변환되지 않음(Pitfall 1). 텍스트는 `addTodo` → `textContent` 경로(02-01에서 mitigate됨). |
| T-02-04 | Repudiation / Logging | save() 실패 처리 | accept (Phase 4 PERS-04로 transfer) | console.warn만 — Phase 4에서 토스트 hook으로 강화 예정. in-memory state는 낙관적 갱신 (CONTEXT 결정). |
</threat_model>

<verification>
**Grep gates (자동):**
- `head -12 js/app.js` byte-equal to Phase 1.
- `grep -q "from './todos.js'" js/app.js` → 통과.
- `grep -q "const TODAY = dateKey()" js/app.js` → 통과.
- `grep -E "innerHTML\s*=|outerHTML\s*=|insertAdjacentHTML" js/app.js` → 0건.
- `grep -E "localStorage\." js/app.js` → 0건.
- `grep -E "toISOString|toJSON|Date\.UTC" js/app.js` → 0건.
- `grep -E "addEventListener\(['\"]keydown['\"]" js/app.js` → 0건 (이 plan에선 추가 안 함; 02-03에서 추가됨).

**브라우저 수동 검증:**
1. `python3 -m http.server 8000` → `http://localhost:8000` 접속.
2. 콘솔 에러 0건. `[hansung-todo] foundation loaded` 부트 로그 1회.
3. 페이지에 input + 추가 버튼 + "아직 할 일이 없습니다." 메시지 노출 (TODO-07 첫 노출).
4. "공부하기" 입력 → Enter → 리스트에 1개 노출, input clear, focus 유지.
5. "운동하기" 입력 → 추가 버튼 클릭 → 리스트에 2개 노출.
6. 빈 문자열/공백만 입력 → 추가 시도 → 변화 없음.
7. **한국어 IME 검증:** "공부하기"를 IME로 입력 — 마지막 자모 조합 확정 Enter가 add를 실행하지 않고 정상 완료된 후의 다음 Enter가 add 트리거. 결과: "공부하기" 그대로 추가, "공부하긱" 같은 깨진 텍스트 없음.
8. F5 reload → 추가된 2개가 동일 순서로 노출.
</verification>

<success_criteria>
- [ ] Phase 1 13줄 보존(append-only).
- [ ] DOM 마운트 1회만, 폼 + 리스트 컨테이너 동적 생성.
- [ ] 최초 렌더 시 empty-state 메시지 노출 (TODO-07 충족).
- [ ] 추가 폼 submit으로 todo 추가 + input clear + focus 유지 (TODO-01).
- [ ] reload 후 데이터 유지 (PERS-03 첫 검증).
- [ ] 한국어 IME 입력 시 잘린 자모 추가 0건.
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-crud/02-02-SUMMARY.md` documenting:
- Modified file: js/app.js (line count before/after, diff summary).
- Confirmed: Phase 1 13줄 byte-equal preservation.
- Browser verification transcript (8 단계).
- Open question for 02-03: editingId guard 위치, 위임 listener 4종 등록 순서.
</output>
