---
phase: 02-todo-crud
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - js/app.js
autonomous: true
requirements: [TODO-02, TODO-03, TODO-04, TODO-05]
tags: [vanilla-js, event-delegation, ime-guard, focusout-reentry-guard, inline-edit]

must_haves:
  truths:
    - "listContainer에 4개 listener(change/click/keydown/focusout) 단 1회 등록"
    - "체크박스는 change 이벤트로만 처리 (click 아님 — Pitfall 3)"
    - "편집 input keydown은 e.isComposing || e.keyCode === 229 가드 후 Enter/Esc 분기"
    - "commitEdit 첫 줄에서 const id = editingId; editingId = null; (focusout 재진입 차단 — Pitfall 2)"
    - "focusout 핸들러는 if (editingId == null) return; 가드"
    - "텍스트 클릭(.todo-text) 또는 ✎(.todo-edit) → startEdit, 🗑(.todo-remove) → removeTodo"
    - "addTodo 후에도 위임 listener는 살아있음 (replaceChildren로 자식만 교체, 컨테이너 보존)"
  artifacts:
    - path: "js/app.js"
      provides: "Phase 2 위임 이벤트 + 인라인 편집 라이프사이클 완성"
      contains: "listContainer.addEventListener('focusout'"
  key_links:
    - from: "js/app.js (focusout listener)"
      to: "commitEdit (editingId guard)"
      via: "editingId === null re-entry guard"
      pattern: "if\\s*\\(editingId\\s*==\\s*null\\)\\s*return"
    - from: "js/app.js (keydown listener)"
      to: "IME guard"
      via: "isComposing / keyCode 229"
      pattern: "isComposing|keyCode\\s*===\\s*229"
---

<objective>
02-02가 마운트한 listContainer에 위임 이벤트 4종(change / click / keydown / focusout)을 1회만 등록하고, 인라인 편집 라이프사이클(startEdit / commitEdit / cancelEdit) 3개 함수를 추가하여 토글/편집/삭제를 완성한다.

Purpose: 사용자가 체크박스 토글, 텍스트 인라인 편집(Enter/Esc/blur, 빈 거부), 즉시 삭제까지 모두 수행할 수 있다 (TODO-02 createdAt 검증, TODO-03/04/05). 한국어 IME 함정과 focusout 재진입 함정을 두 가드로 무력화한다.

Output: `js/app.js` 추가 수정 (02-02 위에 listener 4개 + edit 함수 3개 + import 라인 확장).
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-todo-crud/02-CONTEXT.md
@.planning/phases/02-todo-crud/02-RESEARCH.md
@.planning/phases/02-todo-crud/02-PATTERNS.md
@.planning/phases/02-todo-crud/02-01-SUMMARY.md
@.planning/phases/02-todo-crud/02-02-SUMMARY.md
</execution_context>

<context>
@js/app.js
@js/todos.js

<interfaces>
<!-- 02-01 todos.js exports + 02-02에서 만들어진 모듈 변수. -->

todos.js (02-01):
```js
export function toggleTodo(state, key, id): State;
export function editTodo(state, key, id, text): State;   // text.trim()=='' → state 그대로
export function removeTodo(state, key, id): State;
```

app.js (02-02 마지막 상태에서 추가 import 필요):
```js
let editingId = null;          // 02-02에서 선언됨, 이 plan이 사용
const TODAY = dateKey();        // 02-02에서 선언됨
function commit(nextState);     // 02-02에서 선언됨
function render();              // 02-02에서 선언됨
const listContainer;            // 02-02 buildListContainer로 만들어진 노드
```

DOM contract (02-01 renderTodoItem 합의):
```
<li data-id="...">
  <input class="todo-checkbox" type="checkbox">
  <span class="todo-text"> | <input class="todo-edit-input" type="text">
  <button class="todo-edit">✎</button>
  <button class="todo-remove">🗑</button>
</li>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: js/app.js 확장 — 위임 listener 4종 + startEdit/commitEdit/cancelEdit + import 보강</name>
  <files>js/app.js</files>
  <read_first>
    - .planning/phases/02-todo-crud/02-CONTEXT.md (decisions: 인라인 편집, 토글, 삭제, 즉시 삭제, XSS 잠금)
    - .planning/phases/02-todo-crud/02-RESEARCH.md (§Pattern 2 lines 318-353, lines 355-370 편집 라이프사이클, Pitfall 1·2·3 lines 482-513)
    - .planning/phases/02-todo-crud/02-PATTERNS.md (`js/app.js` 섹션 lines 155-215; 특히 위임 4종 + commit 가드)
    - .planning/phases/02-todo-crud/02-02-SUMMARY.md (현재 app.js 상태)
    - js/app.js (현재 상태 — 02-02 적용 후)
    - js/todos.js (toggleTodo/editTodo/removeTodo 시그니처 재확인)
  </read_first>
  <behavior>
    - 체크박스 클릭 또는 Space(키보드) → 즉시 done 토글 + 시각 구분(li.done 클래스) + reload 후에도 유지.
    - 🗑 버튼 클릭 → confirm 없이 즉시 삭제. 한 번 누르면 undo 불가능.
    - 텍스트(.todo-text) 단일 클릭 또는 ✎ 버튼 클릭 → 같은 자리가 input(.todo-edit-input)으로 치환, focus + caret이 텍스트 끝.
    - 편집 input에서 Enter → 저장 + 편집 종료 (빈/공백만이면 저장 거부, 원래 텍스트 유지).
    - Esc → 취소 + 편집 종료 (편집 시작 전 텍스트 복원).
    - 다른 곳 클릭(blur/focusout) → 저장(commitEdit과 동일 경로, 빈/공백 거부).
    - 한국어 IME 입력 도중 자모 결합 확정 Enter → commit 발화 0회 (isComposing 가드).
    - 한 번에 한 todo만 편집 — 다른 행 텍스트 클릭 시 진행 중 편집은 자동 commit 후 새 편집 시작.
    - commit/cancel 직후 발생하는 focusout(replaceChildren로 input이 detach되며 발화)은 재진입 0회 (editingId == null 가드).
  </behavior>
  <action>
**파일은 02-02가 적용된 상태**(import 라인, window.* 노출, TODAY/state/editingId, DOM 마운트, commit/render, form submit, 최초 render() 호출 모두 존재). 이 plan은 그 위에 다음만 추가:

1) **import 라인 확장** — 기존 `import { getTodosForDate, addTodo, renderTodoList } from './todos.js';`를 다음으로 교체:
```js
import {
  getTodosForDate, addTodo, toggleTodo, editTodo, removeTodo, renderTodoList,
} from './todos.js';
```
(이것이 이 plan에서 02-02 코드를 수정하는 **유일한** 라인이다.)

2) **위임 listener 4종** (PATTERNS lines 155-194, RESEARCH §Pattern 2 lines 318-353) — `form.addEventListener('submit', ...)` 위 또는 아래에 4 블록 추가. 마지막 `render();` 호출보다 위.

```js
// 위임: 체크박스 토글 (change 이벤트 — Pitfall 3)
listContainer.addEventListener('change', (e) => {
  const cb = e.target;
  if (!(cb instanceof HTMLInputElement) || cb.type !== 'checkbox') return;
  const id = cb.closest('li')?.dataset.id;
  if (!id) return;
  commit(toggleTodo(state, TODAY, id));
});

// 위임: 클릭 — 삭제 / 편집 시작
listContainer.addEventListener('click', (e) => {
  const t = e.target;
  if (!(t instanceof HTMLElement)) return;
  const id = t.closest('li')?.dataset.id;
  if (!id) return;
  if (t.matches('.todo-remove')) commit(removeTodo(state, TODAY, id));
  else if (t.matches('.todo-edit') || t.matches('.todo-text')) startEdit(id);
});

// 위임: 편집 input keydown — 한국어 IME 가드 (Pitfall 1)
listContainer.addEventListener('keydown', (e) => {
  const input = e.target;
  if (!(input instanceof HTMLInputElement) || !input.matches('.todo-edit-input')) return;
  if (e.isComposing || e.keyCode === 229) return;
  if (e.key === 'Enter') { e.preventDefault(); commitEdit(input); }
  else if (e.key === 'Escape') { e.preventDefault(); cancelEdit(); }
});

// 위임: 편집 input focusout — 재진입 가드 (Pitfall 2)
listContainer.addEventListener('focusout', (e) => {
  const input = e.target;
  if (!(input instanceof HTMLInputElement) || !input.matches('.todo-edit-input')) return;
  if (editingId == null) return;
  commitEdit(input);
});
```

3) **편집 라이프사이클 함수 3개** (PATTERNS lines 197-212, RESEARCH lines 355-370):
```js
function startEdit(id) {
  editingId = id;
  render();
  const input = listContainer.querySelector('.todo-edit-input');
  if (input) {
    input.focus();
    input.setSelectionRange(input.value.length, input.value.length);
  }
}

function commitEdit(input) {
  const id = editingId; editingId = null;   // ← 가드 먼저 풀기 (Pitfall 2)
  if (id == null) return;
  commit(editTodo(state, TODAY, id, input.value));
}

function cancelEdit() {
  editingId = null;
  render();
}
```

4) **마지막 `render();`는 그대로 유지** — 추가 호출 금지.

**금지 사항** (실패 조건):
- `commitEdit` 첫 줄에서 `editingId = null`을 풀지 않으면 focusout 재진입 발생 — verify 자동 grep gate가 잡음.
- `keydown Enter` 핸들러에 `e.isComposing || e.keyCode === 229` 가드 누락 → 한국어 입력 함정.
- `change` 대신 `click`을 체크박스에 사용 → 키보드 Space 회귀 (Pitfall 3).
- per-`<li>` listener 추가 (anti-pattern). listener는 listContainer에 4개만.
- `editingId`를 `state.todosByDate`에 저장 (transient UI 상태 누출 — anti-pattern).
- 추가 폼 form에 keydown listener 추가 (02-02 form submit이 IME 가드를 자동 처리 — 중복).
- `innerHTML` / `outerHTML` 좌변 0건 유지. raw `localStorage` 0건 유지. UTC 토큰 0건 유지.
- 02-04에서 다룰 CSS 변경을 미리 끌어오지 말 것.
  </action>
  <verify>
    <automated>
      cd /Users/insang/Documents/강의/소프트웨어공학/2026/대면수업용/sdd/gsd/hansung-todo-gsd &amp;&amp; \
      grep -q "toggleTodo, editTodo, removeTodo" js/app.js &amp;&amp; \
      grep -cE "listContainer\.addEventListener\(" js/app.js | grep -q "^[[:space:]]*4$" &amp;&amp; \
      grep -q "addEventListener('change'" js/app.js &amp;&amp; \
      grep -q "addEventListener('click'" js/app.js &amp;&amp; \
      grep -q "addEventListener('keydown'" js/app.js &amp;&amp; \
      grep -q "addEventListener('focusout'" js/app.js &amp;&amp; \
      grep -E "isComposing|keyCode === 229|keyCode==229" js/app.js &amp;&amp; \
      grep -q "if (editingId == null) return" js/app.js &amp;&amp; \
      grep -E "const id = editingId;\s*editingId = null" js/app.js &amp;&amp; \
      grep -q "function startEdit" js/app.js &amp;&amp; \
      grep -q "function commitEdit" js/app.js &amp;&amp; \
      grep -q "function cancelEdit" js/app.js &amp;&amp; \
      grep -q "setSelectionRange" js/app.js &amp;&amp; \
      ! grep -E "innerHTML\s*=|outerHTML\s*=|insertAdjacentHTML" js/app.js &amp;&amp; \
      ! grep -E "localStorage\." js/app.js &amp;&amp; \
      ! grep -E "toISOString|toJSON|Date\.UTC" js/app.js &amp;&amp; \
      ! grep -E "form\.addEventListener\(['\"]keydown['\"]" js/app.js &amp;&amp; \
      head -12 js/app.js | diff - <(printf "import { dateKey } from './dateKey.js';\nimport { KEY, defaultState, load, save } from './storage.js';\n\n// Phase 1: UI 없음. 콘솔에서 검증 체크리스트 2/3번 실행을 가능하게 만드는 dev 노출.\n// Phase 2 도입 시 재검토 (제거 또는 축소).\nwindow.dateKey = dateKey;\nwindow.defaultState = defaultState;\nwindow.load = load;\nwindow.save = save;\nwindow.STORAGE_KEY = KEY;\n\nconsole.info('[hansung-todo] foundation loaded', { today: dateKey() });\n") &amp;&amp; \
      echo PASS
    </automated>
  </verify>
  <done>
    - listContainer.addEventListener 호출 정확히 4건 (change / click / keydown / focusout).
    - import 라인에 toggleTodo / editTodo / removeTodo 추가됨.
    - 함수 3개(startEdit / commitEdit / cancelEdit) 존재.
    - IME 가드 토큰(`isComposing` 또는 `keyCode === 229`) 존재.
    - focusout 재진입 가드(`if (editingId == null) return`) 존재.
    - commitEdit 첫 줄 `const id = editingId; editingId = null;` 패턴 존재.
    - Phase 1 13줄 byte-equal 유지.
    - innerHTML / raw localStorage / UTC 토큰 0건.
    - form에 keydown 리스너 0건.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 편집 input 사용자 입력 → `editTodo` | 두 번째 사용자 텍스트 진입 지점 (수정 시). |
| listContainer 위임 → 임의 자식 노드 | 위임 핸들러가 cb/li/input을 instanceof + matches 이중 가드로 검증해야 함. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | 편집 input → editTodo → renderTodoItem | mitigate | 편집 input은 `input.value` property로만 입력 받고, commit 후 렌더는 02-01의 textContent 경로 재사용. innerHTML 0건. |
| T-02-06 | DoS / 로직 결함 | focusout 재진입 commitEdit 무한루프 | mitigate | `editingId = null`을 commit 진입 첫 줄에 풀고 focusout listener에서 `editingId == null` early-return. (Pitfall 2) |
| T-02-07 | 입력 신뢰성 회귀 | 한국어 IME 도중 Enter 발화 | mitigate | `e.isComposing || e.keyCode === 229` 가드. (Pitfall 1) 추가 폼은 `<form>+submit`이 자동 처리. |
</threat_model>

<verification>
**Grep gates (자동):**
- `listContainer.addEventListener(` 등장 4회.
- `change` / `click` / `keydown` / `focusout` 각 1회.
- `isComposing` 또는 `keyCode === 229` 존재.
- `if (editingId == null) return` 존재.
- `const id = editingId; editingId = null` 패턴 존재.
- `setSelectionRange` 존재.
- `form.addEventListener('keydown'` 0건 (있으면 IME 함정).
- `innerHTML` / `outerHTML` / raw `localStorage` / `toISOString` / `Date.UTC` 모두 0건.

**브라우저 수동 검증 시나리오:**
1. 4개 todo 추가: "공부", "운동", "독서", "쇼핑".
2. 두 번째 행 체크박스 클릭 → strikethrough 적용 (CSS는 02-04 적용 후 시각 확인). dataset 측면: DevTools에서 `<li data-id="...">`에 `done` 클래스 추가 확인.
3. 키보드: 세 번째 행 체크박스에 Tab 후 Space → 토글. (Pitfall 3 회귀 검증.)
4. 첫 행 텍스트("공부") 클릭 → input으로 치환, caret 끝, "수업" 추가 입력 → "공부수업".
5. Enter → 저장. reload 후에도 "공부수업" 유지.
6. 두 번째 행 ✎ 버튼 클릭 → input. 빈 문자열로 지우고 Enter → 저장 거부, "운동" 그대로 유지.
7. 세 번째 행 텍스트 클릭 → input → "독서일기" 입력 → 다른 곳 클릭(blur) → "독서일기" 저장. 콘솔에 warn 1회 미만(0회).
8. 네 번째 행 텍스트 클릭 → input → "쇼핑목록" 입력 → Esc → "쇼핑" 그대로 (취소 동작).
9. 첫 행 🗑 클릭 → 즉시 사라짐. 확인 다이얼로그 0회.
10. **한국어 IME 회귀:** 빈 todo 추가용 input은 02-02에서 검증. 인라인 편집에서도 "공부하기" IME 입력 후 Enter — 마지막 자모 확정용 첫 Enter는 commit 안 됨, 다음 Enter가 commit. 결과: "공부하기" 그대로 저장.
11. **focusout 재진입 회귀:** 편집 input에서 텍스트 입력 후 다른 행 텍스트 클릭 (한 번에 한 todo 편집) → 첫 행 commit 후 새 행 startEdit. 콘솔 warn 0회. localStorage `hansung-todo:v1` 한 번만 갱신.
12. reload → 모든 변경 유지(완료, 수정, 삭제 후 남은 3개).
</verification>

<success_criteria>
- [ ] 위임 listener 4종 등록.
- [ ] 토글/편집/삭제 모두 동작 + reload 유지.
- [ ] 한국어 IME 입력 시 잘린 자모 추가/편집 0건.
- [ ] focusout 재진입 commit 0건 (localStorage 갱신 1회만).
- [ ] 빈 문자열 편집 거부 (원래 텍스트 유지).
- [ ] Phase 1 13줄 보존.
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-crud/02-03-SUMMARY.md` documenting:
- Modified file: js/app.js (final line count, listener count, function count).
- 12 단계 수동 검증 결과 (각 PASS/FAIL).
- DevTools localStorage 다이프 횟수 측정 (focusout 재진입 0회 검증 증거).
- Open question for 02-04: CSS 미니멀 룰 3종 / phase-level must_haves 합산.
</output>
